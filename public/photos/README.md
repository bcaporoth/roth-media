# Photos

Drop portfolio images in this folder (`.jpg`, `.png`, `.webp`, or `.avif`).

They appear automatically in the "Selected Work" grid on the homepage,
sorted by filename — prefix with numbers to control order
(e.g. `01-flower-shop.jpg`, `02-product-detail.jpg`).

Until photos are added, the site shows styled placeholder tiles.
